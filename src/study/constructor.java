package study;

public class constructor {

    // 객체를 만드는 특별한 메서드 = 생성자
    // 생성자 : 클래스로부터 객체를 만드는 '특별한 메서드'

    // 클래스를 통해 객체를 만들기 위해선 생성자를 호출해야함
    // ex. Cat cat1 - new Cat():  >> 여기서 생성자는 Cat() 임

    // 생성자도 메서드이기 때문에 생성자를 호출할 때에는 소괄호가 열고 닫히는 형태를 가짐

    // 생성자의 역할 :
    // 1. 객체 생성 2. 초기화

    // 생성자는 초기화의 역할도 수행 가능.
    // 보통 객체를 생성하면 일반적으로 필드를 초기화 함.

//    Cat nero = new Cat();  >> 객체 생성 (호출)
//
    // 필드 초기화 (초기값 설정)  (정의)
//    nero.name = "네로";
//    nero.weight = 3.87;
//    nero.age = 3;

// 생성자를 잘 활용하면 위에 코드들을 한 줄로 줄일 수 있듬 !
//    Cat cat1 = new Cat("네로", 3.87, 3);


// 생성자 사용법 (호출 -> 정의) > 메서드이기 때문.

    // 호출
    // 클래스_타입 변수명 = new 클래스_이름(전달값);

    // 정의
    // 클래스_이름 (파라미터 {
    //   ...
    // }
    // 정의할 땐 리턴 타입 안 적는다 !!!!!!!!!!!!!!!

    // 생성자는 클래스 명과 같아야함
    // 생성자는 클래스 명과 같아야함
    // 생성자는 클래스 명과 같아야함
    // 호출에 new 쓰는거랑 정의에 return 안적는 거 꼭 기억하기
    // 호출에 new 쓰는거랑 정의에 return 안적는 거 꼭 기억하기
    // 호출에 new 쓰는거랑 정의에 return 안적는 거 꼭 기억하기


//Public clas CatTest {
//    public static void main(String[] args) {
//        Cat cat1 = new Cat("neko", 3.87, 3);
//        }
//    }
//
//    class Cat {
//    String name;
//    double weight;
//    int age;
//
//    Cat(String s, double d, int i) {
//        name = s;   이름 초기화
//        weight d ;  무게 초기화
//        age = i;    나이 초기화
//    }
//    }








}
